include(CMakeFindDependencyMacro)

if("${QT_MAJOR_VER}" STREQUAL "6")
    find_dependency(Qt6 REQUIRED COMPONENTS Core Sql CONFIG)
else()
    find_dependency(Qt5 REQUIRED COMPONENTS Core Sql CONFIG)
endif()

find_dependency(VeinFramework REQUIRED)
find_dependency(VfQmlLibs REQUIRED)

include("${CMAKE_CURRENT_LIST_DIR}/VfLoggerTargets.cmake")
